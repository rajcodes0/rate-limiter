server {
  listen 80;
  location / { try_files $uri $uri/ /index.html; }
  # Keep browser and API on one origin in Docker; nginx forwards dashboard calls.
  location /api/ { proxy_pass http://api:3000; }
}
